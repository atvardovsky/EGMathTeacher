# HTTP port for NestJS app
PORT=3000

# Optional HTTPS cert paths (enable HTTPS when both are set)
HTTPS_KEY_PATH=
HTTPS_CERT_PATH=

# Comma-separated list of allowed CORS origins (use * to allow any origin without credentials)
CORS_ORIGINS=https://atvardovsky.dev,https://www.atvardovsky.dev,https://localhost:5137,http://localhost:5137
# Allow sending credentials (cookies/Authorization headers) with CORS requests
CORS_CREDENTIALS=true

# Local POC auth/session storage
SQLITE_PATH=./data/app.sqlite
JWT_SECRET=change-me-before-sharing
AUTH_COOKIE_NAME=egmathteacher_session
# Use true for HTTPS production on atvardovsky.dev.
AUTH_COOKIE_SECURE=false
AUTH_SESSION_DAYS=7

# Lesson lifecycle limits are POC teaching heuristics, not medical fatigue assessment.
LESSON_DAILY_SOFT_LIMIT_MINUTES=90
LESSON_DAILY_HARD_LIMIT_MINUTES=120
LESSON_CONTINUOUS_SOFT_LIMIT_MINUTES=45
LESSON_CONTINUOUS_HARD_LIMIT_MINUTES=60
LESSON_MIN_TURN_SECONDS=30
LESSON_MAX_TURN_GAP_SECONDS=900

# Require imported task-bank rows for supported verifier lessons instead of
# using the generated POC fallback task.
TASK_BANK_REQUIRED=false
# Require active curriculum_mastery_criteria for supported verifier skills.
MASTERY_CRITERIA_REQUIRED=true

# Realtime voice provider selection (openai-realtime | google-gemini-live | hume-evi | retell-ai)
AI_PROVIDER=openai-realtime

# Text/RAG/image/file model provider selection. Only openai is implemented in this POC.
AI_MODEL_PROVIDER=openai

# OpenAI realtime API key (server-side only)
OPENAI_API_KEY=
# Model alias for OpenAI realtime sessions
OPENAI_REALTIME_MODEL=gpt-4o-realtime-preview
# Model used for RAG tutor responses
OPENAI_RESPONSES_MODEL=gpt-5.5
# Model used when the tutor generates explanatory diagrams
OPENAI_IMAGE_MODEL=gpt-image-2
OPENAI_IMAGE_SIZE=1024x1024
OPENAI_IMAGE_QUALITY=low

# Optional per-role/per-operation model overrides. Empty values fall back to
# OPENAI_RESPONSES_MODEL, OPENAI_IMAGE_MODEL, or OPENAI_BACKGROUND_* defaults.
AI_OPERATION_LESSON_DECISION_MODEL=
AI_OPERATION_TUTOR_ANSWER_MODEL=
AI_OPERATION_TUTOR_ANSWER_WITH_RAG_MODEL=
AI_OPERATION_TUTOR_IMAGE_MODEL=
AI_OPERATION_ONBOARDING_KNOWLEDGE_MODEL=
AI_OPERATION_ONBOARDING_PSYCHOPEDAGOGICAL_MODEL=
AI_OPERATION_ONBOARDING_STRATEGY_MODEL=
AI_OPERATION_BACKGROUND_LEARNING_SIGNAL_MODEL=
AI_OPERATION_BACKGROUND_LEARNING_WINDOW_MODEL=
AI_OPERATION_BACKGROUND_SESSION_SUMMARY_MODEL=
AI_OPERATION_BACKGROUND_PROFILE_REFRESH_MODEL=
AI_OPERATION_BACKGROUND_TEACHING_STRATEGY_REFRESH_MODEL=
AI_OPERATION_BACKGROUND_PROFILE_STRATEGY_REFRESH_MODEL=
AI_OPERATION_BACKGROUND_QUALITY_REVIEW_MODEL=

# Optional per-operation Responses API service tiers. Empty tutor/onboarding
# values use the standard tier; empty background values fall back to
# OPENAI_BACKGROUND_SERVICE_TIER.
AI_OPERATION_LESSON_DECISION_SERVICE_TIER=
AI_OPERATION_TUTOR_ANSWER_SERVICE_TIER=
AI_OPERATION_TUTOR_ANSWER_WITH_RAG_SERVICE_TIER=
AI_OPERATION_ONBOARDING_KNOWLEDGE_SERVICE_TIER=
AI_OPERATION_ONBOARDING_PSYCHOPEDAGOGICAL_SERVICE_TIER=
AI_OPERATION_ONBOARDING_STRATEGY_SERVICE_TIER=
AI_OPERATION_BACKGROUND_LEARNING_SIGNAL_SERVICE_TIER=
AI_OPERATION_BACKGROUND_LEARNING_WINDOW_SERVICE_TIER=
AI_OPERATION_BACKGROUND_SESSION_SUMMARY_SERVICE_TIER=
AI_OPERATION_BACKGROUND_PROFILE_REFRESH_SERVICE_TIER=
AI_OPERATION_BACKGROUND_TEACHING_STRATEGY_REFRESH_SERVICE_TIER=
AI_OPERATION_BACKGROUND_PROFILE_STRATEGY_REFRESH_SERVICE_TIER=
AI_OPERATION_BACKGROUND_QUALITY_REVIEW_SERVICE_TIER=

# Lesson Decision Agent can be disabled for latency/cost experiments. When
# enabled, this timeout controls when the tutor path falls back to local safe
# decision guidance.
AI_LESSON_DECISION_ENABLED=true
AI_LESSON_DECISION_TIMEOUT_MS=3500

# Background profile/signal assistants run after the immediate answer.
AI_BACKGROUND_ENABLED=true
# Store tutor-turn observations and analyze them in grouped windows instead of
# calling the signal extractor after every turn. Set false to restore legacy
# per-turn background extraction.
AI_BACKGROUND_BATCHING_ENABLED=true
OPENAI_BACKGROUND_RESPONSES_MODEL=
OPENAI_BACKGROUND_WINDOW_RESPONSES_MODEL=gpt-5.4-nano
OPENAI_BACKGROUND_REFRESH_RESPONSES_MODEL=gpt-5.4-mini
OPENAI_BACKGROUND_SERVICE_TIER=flex
AI_BACKGROUND_PROMPT_CACHE_KEY_ENABLED=true
AI_BACKGROUND_DRAIN_INTERVAL_MS=2000
AI_BACKGROUND_DRAIN_BATCH_SIZE=3
AI_BACKGROUND_MAX_ATTEMPTS=2
AI_BACKGROUND_OBSERVATION_WINDOW_SIZE=10
AI_BACKGROUND_OBSERVATION_MAX_WINDOW_SIZE=12
AI_BACKGROUND_OBSERVATION_IDLE_FLUSH_MS=900000
AI_BACKGROUND_RUNNING_JOB_TIMEOUT_MS=600000
AI_BACKGROUND_PROFILE_REFRESH_TURN_INTERVAL=10
AI_BACKGROUND_SESSION_SUMMARY_TURN_INTERVAL=5

# User-visible usage estimates. Prices are local configuration, not provider billing proof.
# Leave zero when pricing is not configured; the UI will still show tokens/images.
AI_USAGE_TRACKING_ENABLED=true
AI_USAGE_DEFAULT_INPUT_USD_PER_1M=0
AI_USAGE_DEFAULT_CACHED_INPUT_USD_PER_1M=0
AI_USAGE_DEFAULT_OUTPUT_USD_PER_1M=0
AI_USAGE_DEFAULT_IMAGE_USD=0
# Optional per-model override JSON, for example:
# {"gpt-5.5":{"inputUsdPer1M":0,"outputUsdPer1M":0,"cachedInputUsdPer1M":0,"source":"local_config"}}
# Service-tier overrides can use "<model>:<tier>" keys, for example:
# {"gpt-5.5:flex":{"inputUsdPer1M":0,"outputUsdPer1M":0,"cachedInputUsdPer1M":0,"source":"local_config_flex"}}
# Image generation can use token pricing too. When an image response omits
# usage tokens, the POC estimates gpt-image-2 output tokens from configured
# size/quality, for example low 1024x1024 -> 196 output tokens.
AI_USAGE_MODEL_PRICING_JSON=
# Comma-separated OpenAI vector store ids. If empty, admin upload creates/reuses one.
# The local knowledge-pack sync command also records the created/reused vector store in SQLite
# when OPENAI_VECTOR_STORE_IDS is empty.
OPENAI_VECTOR_STORE_IDS=
KNOWLEDGE_RAG_INDEX_WAIT_ATTEMPTS=40
KNOWLEDGE_RAG_INDEX_WAIT_DELAY_MS=500
# Optional transcription model used to auto-transcribe caller audio (defaults to gpt-4o-mini-transcribe)
OPENAI_INPUT_TRANSCRIPTION_MODEL=gpt-4o-mini-transcribe

# Google Gemini Live (optional)
GEMINI_API_KEY=
GEMINI_LIVE_MODEL=

# Hume EVI (optional)
HUME_API_KEY=
HUME_SECRET_KEY=

# Retell AI (optional)
RETELL_API_KEY=

# Optional comma-separated ICE server URLs (STUN/TURN) for WebRTC clients
WEBRTC_ICE_SERVERS=stun:stun.l.google.com:19302
# Safeguard for the number of concurrent WebRTC sessions tracked in-memory
WEBRTC_MAX_SESSIONS=25
# Enable barge-in (cancel current model response when caller starts speaking)
WEBRTC_ENABLE_BARGE_IN=true
# Close inactive sessions after this many milliseconds
WEBRTC_SESSION_IDLE_TIMEOUT_MS=300000
# Frequency of idle-session sweeps in milliseconds
WEBRTC_IDLE_SWEEP_INTERVAL_MS=60000
# Timeout/retry tuning for OpenAI REST calls used by the bridge
OPENAI_REQUEST_TIMEOUT_MS=10000
OPENAI_REQUEST_RETRIES=2
# Refresh OpenAI client secrets this many milliseconds before expiry
OPENAI_CLIENT_SECRET_GRACE_MS=5000

# Directory where finalized conversation transcripts are stored
TRANSCRIPT_LOG_DIR=./logs

# Assistant personality configuration
ASSISTANT_PERSONALITY_NAME=Voice Assistant
ASSISTANT_PERSONALITY_DESCRIPTION=A helpful, conversational voice companion.
ASSISTANT_PERSONALITY_TONE=friendly
# Use `auto` to let the assistant infer locale from caller context
ASSISTANT_PERSONALITY_LOCALE=auto
# Use plain text to describe core behavioral rules (referenced in Realtime sessions)
ASSISTANT_RULES=Stay polite, concise, and clarify when unsure.

# Assistant voice configuration
ASSISTANT_DEFAULT_VOICE=alloy
ASSISTANT_AVAILABLE_VOICES=alloy,verse,sol

# OpenAI File Search attachments
# Provide comma-separated OpenAI file IDs used for documentation grounding
FILE_SEARCH_DOCUMENTATION_IDS=
# Provide comma-separated OpenAI file IDs used for rule compliance
FILE_SEARCH_RULE_IDS=
